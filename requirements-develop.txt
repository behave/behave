# ============================================================================
# PYTHON PACKAGE REQUIREMENTS: For development only
# ============================================================================
# REQUIRES: Python >= 2.5
# REQUIRES: Python >= 3.2
# DESCRIPTION:
#    pip install -r <THIS_FILE>
#
# ============================================================================

# -- TESTS:
nose  >= 1.1
mock  >= 0.8
PyHamcrest >= 1.6

# -- DEVELOPMENT SUPPORT:
paver >= 1.0.5
tox   >= 1.4
nose-cov >= 1.4
coverage >= 3.5

# -- DOCUMENTATION: Tutorials with examples.
sphinx >= 1.1
sphinxcontrib-cheeseshop >= 0.2
